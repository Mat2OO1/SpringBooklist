CREATE TABLE Booklist(
                       id int NOT NULL PRIMARY KEY,
                       name VARCHAR(100) NOT NULL,
                       author VARCHAR(100),
                       rating int
)